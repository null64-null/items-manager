name: test_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter_riverpod:
  flutter:
    sdk: flutter
  sqflite:
  path:
  badges: ^3.1.1
  cupertino_icons: ^1.0.2
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

flutter_icons:
  image_path: "assets/images/zaiko-icon.png"
  ios: true
  android: true