import 'package:flutter/material.dart';

class ShoppingPlaceListPage extends StatelessWidget {
  const ShoppingPlaceListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Shopping Place List');
  }
}
